'use strict';

angular.module('ppmApp')
    .factory('District', function ($resource, DateUtils) {
        return $resource('api/districts/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
